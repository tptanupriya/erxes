import { Alert as CommonAlert} from 'erxes-common-ui'

const Alert = {
  success: CommonAlert.success,
  error: CommonAlert.error,
  warning: CommonAlert.warning,
  info: CommonAlert.info
};

export default Alert;
