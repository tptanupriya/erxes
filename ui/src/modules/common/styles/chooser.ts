import { chooser } from 'erxes-common-ui'

const Columns = chooser.Columns;

const Column = chooser.Column;

const Title = chooser.Title;

const Footer = chooser.Footer;

const Select = chooser.Select;

const ActionTop = chooser.ActionTop;

export { Columns, Column, Title, Footer, Select, ActionTop };
